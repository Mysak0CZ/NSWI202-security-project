{
	"name": "mysak-security-project-server",
	"version": "1.0.0",
	"private": true,
	"author": "Michal Pácal",
	"license": "UNLICENSED",
	"type": "module",
	"main": "./out/index.js",
	"scripts": {
		"build": "pnpm run --silent clean && tsc -p ./tsconfig.json",
		"dev": "tsc -p ./tsconfig.json -w",
		"clean": "rimraf out"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"rimraf": "^6.0.1",
		"tslib": "^2.8.1",
		"typescript": "^5.7.2"
	}
}
